# Wajib untuk Windows Server / XAMPP — hindari npm memilih @tailwindcss/oxide-wasm32-wasi
# (sering terjadi jika npm dijalankan dari Git Bash MINGW64, bukan PowerShell/CMD)
os=win32
cpu=x64
legacy-peer-deps=true
